# 网站主站 - iice.fun
server {
    listen 80;
    server_name iice.fun;

    # 日志配置
    access_log /var/log/nginx/iice.fun_access.log;
    error_log /var/log/nginx/iice.fun_error.log;

    # 代理配置
    location / {
        proxy_pass http://127.0.0.1:9900;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket支持（如果需要）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # 超时配置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# SSL配置示例（取消注释并配置证书后启用）
# server {
#     listen 443 ssl http2;
#     server_name iice.fun;
# 
#     ssl_certificate /etc/nginx/ssl/iice.fun.crt;
#     ssl_certificate_key /etc/nginx/ssl/iice.fun.key;
#     
#     # SSL优化配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
# 
#     # 日志配置
#     access_log /var/log/nginx/iice.fun_ssl_access.log;
#     error_log /var/log/nginx/iice.fun_ssl_error.log;
# 
#     location / {
#         proxy_pass http://127.0.0.1:9900;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         
#         # WebSocket支持
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         
#         # 超时配置
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
# }

# Minio图床 - img.iice.fun（只读图片访问）
server {
    listen 80;
    server_name img.iice.fun;

    # 日志配置
    access_log /var/log/nginx/img.iice.fun_access.log;
    error_log /var/log/nginx/img.iice.fun_error.log;

    # 图片资源代理
    location / {
        proxy_pass http://127.0.0.1:9901;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 缓存配置
        expires 30d;
        add_header Cache-Control "public, immutable";
        
        # 超时配置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# SSL配置示例（取消注释并配置证书后启用）
# server {
#     listen 443 ssl http2;
#     server_name img.iice.fun;
# 
#     ssl_certificate /etc/nginx/ssl/img.iice.fun.crt;
#     ssl_certificate_key /etc/nginx/ssl/img.iice.fun.key;
#     
#     # SSL优化配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
# 
#     # 日志配置
#     access_log /var/log/nginx/img.iice.fun_ssl_access.log;
#     error_log /var/log/nginx/img.iice.fun_ssl_error.log;
# 
#     # 图片资源代理
#     location / {
#         proxy_pass http://127.0.0.1:9901;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         
#         # 缓存配置
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#         
#         # 超时配置
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
# }

